<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="StrPrdtCommon">
	
	<sql id="pageHeader">
		SELECT B.*
			 , B.TOTAL_COUNT
		     , CEIL(B.TOTAL_COUNT / #{strPrdtViewCnt}) -1 LAST_PAGE
		     , FLOOR(FLOOR(B.TOTAL_COUNT / #{strPrdtViewCnt}) / #{strPrdtPageCnt}) LAST_GROUP
		  FROM (SELECT R.*
		             , COUNT(1) OVER() TOTAL_COUNT
		          FROM (
	</sql>
	
	<sql id="pageFooter">
						) R) B
 		 WHERE RNUM BETWEEN (#{strPrdtPageNo} * #{strPrdtViewCnt}) + 1 
 						AND (#{strPrdtPageNo} * #{strPrdtViewCnt}) + #{strPrdtViewCnt}
	</sql>
	
</mapper>